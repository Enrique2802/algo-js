let min = Number(prompt("Enter min:"));

undefined
let max = Number(prompt("Enter max"));
undefined
let current = Number(prompt("Enter current"));
undefined
if (current < max && current > min) {console.log("Current is between min and max")};

Current is between min and max

